# Updating Mockito to a version compatible with Java 17

If org.mockito:mockito-core is defined as a dependency ensure the version is at least 4.11.0 .

If org.mockito:mockito-all is defined as a dependency replace it with org.mockito:mockito-core:4.11.0 .
